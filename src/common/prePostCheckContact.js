// // import { getStoredState } from "redux-persist";

// export const checkContactIsAdded = (contact, contacts) => {
//   // const contacts= getState
//   const nameContact = contact.name.trim().toUpperCase();
//   const findContact = contacts.find(
//     ({ name }) => name.trim().toUpperCase() === nameContact
//   );
//   if (findContact) {
//     return true;
//   } else return false;
// };
